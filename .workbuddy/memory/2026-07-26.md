# 2026-07-26 工作日志

- **清理旧测试 Release/tag（00:44 用户指令"删除旧的 Release"）**：通过 GitHub API（钥匙串 PAT + `releases/{id}` DELETE + `git/refs/tags/...` DELETE）删除 `v0.0.0-ci-test` Release(id 359792991, HTTP 204) 与其 tag(HTTP 204)；本地 `git tag -d v0.0.0-ci-test`。远程 tag 现仅 `v0.1.0`。仓库不再有冗余测试 Release。

- **新增本地发布脚本 `scripts/release-local.sh`（00:44 用户"给出如何本地 release 的一个脚本，注意需要有版本升级用了什么东西"）**：在本地 Mac 复刻 CI 出包（xcodegen generate → 未签名 xcodebuild Release → hdiutil DMG），并自动升版本。**版本升级来源**：`AppLaunchpad/Info.plist` 的 `CFBundleShortVersionString`（对外版本，如 0.1.0，对应 git tag `vX.Y.Z`）+ `CFBundleVersion`（内部构建号，每发版 +1）。用法：`./scripts/release-local.sh [major|minor|patch|<x.y.z>] [--no-bump] [--tag]`；`--tag` 仅打**本地** git tag（不推送，避免触发 CI 重复发版，要发 GitHub Release 再 `git push origin vX.Y.Z`）。已 `chmod +x` + `zsh -n` 语法校验通过 + 单独验证了版本号升级分支逻辑。脚本未自动运行（会改动 Info.plist 升版 + 数分钟构建，属副作用，留给用户手动触发）。待提交：`scripts/release-local.sh`（新建未跟踪）。

- **新增 `gh` 发版脚本 `scripts/release-gh.sh` + 起始 `CHANGELOG.md`（01:31 用户"给 gh release create ... --notes-file CHANGELOG.md 写个 sh 脚本，版本号支持参数，前几行可配置 notes-file 路径，默认 CHANGELOG.md"）**：`#!/bin/sh` POSIX 脚本，前几行配置 `NOTES_FILE="CHANGELOG.md"`（可改）与 `TITLE_PREFIX`（留空则标题=标签名）；参数传版本号（支持 `0.2.0`/`v0.2.0`，统一归一为 `vX.Y.Z`）；含 gh 安装检查、notes 文件存在检查、Info.plist 版本与 tag 不一致软提醒（3s 后可继续）；核心命令 `gh release create "$TAG" --title "$TITLE" --notes-file "$NOTES_FILE"`（自动建 tag 并推 → 触发 CI 幂等上传 DMG）。另建 `CHANGELOG.md`（含 v0.1.0 记录 + v0.2.0 待填模板）。已 `chmod +x` + `sh -n` 语法校验通过 + 验证参数解析。注：本机未装 gh，脚本运行时会提示先 `brew install gh && gh auth login`。待提交：`scripts/release-gh.sh`、`CHANGELOG.md`（均新建未跟踪）。

- **发版相关文件整理到 `scripts/release/`（01:35 用户"changelog 也需要放到脚本相关的目录 scripts/release 都整理放到这里"）**：将 `scripts/release-local.sh`、`scripts/release-gh.sh`、`CHANGELOG.md` 三个文件统一移入 `scripts/release/`（用 git mv 失败回退 mv，因当时均未被跟踪）。同步修正脚本内路径解析：① `release-local.sh` 的 `cd "$SCRIPT_DIR/.."` → `cd "$SCRIPT_DIR/../.."`（脚本下沉两级）；② `release-gh.sh` 新增 `SCRIPT_DIR`/`REPO_ROOT` 解析（回退两级到仓库根），`NOTES_FILE` 默认改为 `"$SCRIPT_DIR/CHANGELOG.md"`（脚本同目录），`INFO_PLIST` 改为 `"$REPO_ROOT/AppLaunchpad/Info.plist"`，并同步更新所有用法注释里的路径为 `scripts/release/...`。校验：`sh -n`/`zsh -n` 语法通过，路径解析测试 `REPO_ROOT`→仓库根、`NOTES_FILE`→`scripts/release/CHANGELOG.md`、`Info.plist` 存在。两者目前仍未提交（用户可决定何时 commit+push）。

- **增强 `release-gh.sh`（02:13 用户"让 release-gh.sh 顺手改 Info.plist 版本号；不带 0.2.0 参数时自动加一"）**：改为① 不传版本参数时按脚本顶部 `BUMP`（默认 minor）在当前 Info.plist 版本上自动加一（0.1.0→0.2.0），传 `0.2.0`/`v0.2.0` 则按指定；② 用 PlistBuddy 写回 `CFBundleShortVersionString`+`CFBundleVersion`(+1)；③ `AUTO_COMMIT=1` 时自动 `git add/commit/push` Info.plist；④ `gh release create` 建 tag 触发 CI（彻底替代本机 `release-local.sh`）。语法校验 + 加一逻辑 mock 验证通过（minor→0.2.0 / patch→0.1.1 / major→1.0.0，且未改真实文件）。

- **新增 AI 发版流程文档 `scripts/release/RELEASE.md`（02:21 用户"写一个让 ai 看的文档，后续 ai 根据这个文档升级版本打包"）**：详述版本真源=`AppLaunchpad/AppLaunchpad/Info.plist`（两层同名目录）、发版只需 `./scripts/release/release-gh.sh [版本]`、改动文件清单、等价命令、前置条件、固定下载地址 `…/releases/latest/download/AppLaunchpad.dmg`、红线坑（DMG 名不变/不标 pre-release/版本只增/layout.json 的 version 非 app 版本/bundle id 固定/未签名安装需清 quarantine+授权辅助功能）、本地打包备选与异常处理。

- **新增一键安装脚本 `scripts/release/install.command` 并打进 DMG（02:32 用户"落地"）**：`install.command` 双击由 Terminal 运行，`sudo bash -c` 把 app 装到 `/Applications/AppLaunchpad.app` 并 `xattr -r -d com.apple.quarantine` + `spctl --add`（后者最佳努力）；含 app 不存在/正在运行校验；安装后仅打印成功行 + 首次启动「仍要打开」提示（**不含**辅助功能自动开启——用户打开 app 后在系统设置自行授权）。改动 `.github/workflows/build-dmg.yml`：Package DMG 步 `cp scripts/release/install.command "$STAGING/"` 把脚本打进 DMG；Release 说明改为「方式一 双击 install.command / 方式二 手动 xattr」。已 `bash -n` 语法校验 + `chmod +x` + ruby YAML 校验通过。提交 `6992fc0`（install.command + build-dmg.yml + 已在暂存的 RELEASE.md/release-gh.sh/MEMORY.md）。未提交遗留：`AppLaunchpad/Views/Settings/AboutPane.swift` 有文案微调（"拼音搜索/多显示器同步"→"模糊搜索/多显示器支持"），本次有意排除、未提交。

## 开源与文档（02:35 用户"加 README + 非商业协议"）
- 新增 `README.md`：项目简介、功能清单、环境要求、构建运行（xcodegen generate → open）、辅助功能授权说明、许可证指向、目录结构。
- 新增 `LICENSE`：**自定义「非商业使用许可协议」**——非商业可免费使用/学习/修改/免费分发；**商业使用需著作权人书面授权**；分发须保留版权声明与协议；附无担保/终止/管辖条款。**非 OSI 开源许可证**（区别在于禁未授权商用）。文末留**联系方式占位**（邮箱/其他），需用户填实。

## 发版 v0.2.0（02:38 用户"发一个新版本，写好说明 我测试下"）
- 写好 `scripts/release/CHANGELOG.md` 的 v0.2.0 说明（搜索分页 / 一键安装脚本 install.command / 设置窗口入口优化 / 固定下载地址），提交 `c2eff38`。
- 容器环境装上 `gh`（`brew install gh`），并用钥匙串 PAT 非交互登录（`TOKEN=$(security find-internet-password -s github.com -w); printf '%s' "$TOKEN" | gh auth login --with-token`）→ `gh auth setup-git` 接管 git 凭证；账户 YunyuVv，token scopes 含 repo+workflow（足够发版+触发 Actions）。
- **修复 `release-gh.sh` 崩溃坑（重要，必记）**：脚本原 `set -eu`，在 macOS 自带 `/bin/bash 3.2` 下对 `TAG` 变量诡异报 `unbound variable`。bash -x trace 显示第 31 行 `TAG=v0.2.0` 赋值成功、131 行 `TITLE="$TAG"` 也能展开成 v0.2.0，但 134 行 `echo "...$TAG..."` 却 unbound（同一变量两次引用中间无改动）。排除零宽字符（perl 扫描无、od 确认 `$TAG` 字节干净）后定位为 **bash 3.2 的 `set -u` 怪癖**。修复：去 `set -u` 仅留 `set -e`（已 `bash -n` + 实跑验证通过），提交 `c6b6c28`。**后续维护该脚本不要再加回 `set -u`**；若坚持要用，需改用新版 bash（`#!/usr/bin/env bash` 且本机装 brew bash）规避。
- 发版：`./scripts/release/release-gh.sh 0.2.0` → PlistBuddy 写 Info.plist(0.2.0 / build 4) → git commit+push → `gh release create v0.2.0 --title v0.2.0 --notes-file CHANGELOG.md` 建 tag+Release → CI `build-dmg.yml` 触发（run #30170273167，约 1.5min 成功）→ DMG `AppLaunchpad.dmg`(≈2.5MB) 已上传到 Release。
- 下载地址：永久 `https://github.com/YunyuVv/AppLaunchpad/releases/latest/download/AppLaunchpad.dmg`；本次 `https://github.com/YunyuVv/AppLaunchpad/releases/download/v0.2.0/AppLaunchpad.dmg`。DMG 内已含 `install.command`，挂载后双击即一键安装到 /Applications。
- 注：本次因两次调试跑（首次自动 bump 到 0.2.0、第二次指定 0.2.0 又 bump build）共产生 3 个 bump commit（build 2/3/4：26c6c47 / 4079e21 / 8ccde2a），显示版本均 0.2.0，无功能影响，未 force-push 清理。

## 分发方案修正：install.command → README.txt（03:04）
- **结论（用户实测截图确认）**：未签名的 `.command` 文件被 Gatekeeper 拦截比 `.app` 还死——双击弹出"无法验证…恶意软件"弹窗，**只有"完成/移到废纸篓"，没有"仍要打开"按钮**，无法一键放行。`.command` 的"图形化双击"卖点完全失效，不比直接操作 `.app` 简单。
- **改法**：① 新建 `scripts/release/README.txt`（单流程三步：拖到 /Applications → 终端执行 `sudo xattr -r -d com.apple.quarantine /Applications/AppLaunchpad.app` → 首次打开点"打开"；末尾补辅助功能授权提示）；② `build-dmg.yml` 的 Package DMG 步 `cp scripts/release/install.command` → `cp scripts/release/README.txt`（不再打 install.command 进 DMG）；③ Release NOTES 改为"读 README.txt 按三步安装"。`install.command` 文件保留在 `scripts/release/` 不删（备用，但未签名下不可用）。
- **已发布 v0.2.1**：用户手动把 Info.plist bump 到 0.2.1 / build 5、commit+push、`git tag v0.2.1` 推送触发 CI。CI run #30171678529 成功，Release v0.2.1 资产含 `AppLaunchpad.dmg`（≈2.5 MB）。DMG 内已是 `README.txt`（非 install.command）。Release 说明已用 `gh release edit --notes-file CHANGELOG.md` 更新为 v0.2.1 内容（最初因手动打 tag 走 CI 模板占位文案，已补正）。

## CHANGELOG 双文件分工（03:24）
- 用户要求：`CHANGELOG.md` = **每次发布的说明（不含历史）**；完整累计历史放到新文件 `CHANGELOG-ALL.md`。
- 已执行：① 新建 `CHANGELOG-ALL.md`（含 v0.2.0 + v0.1.0 完整历史，最新在上）；② 改写 `CHANGELOG.md` 只留 v0.2.0（去掉 v0.1.0 历史），顶部注明"只放当次要发布版本、完整历史见 CHANGELOG-ALL.md"；③ 同步更新 `RELEASE.md` 步骤 1 与"改动文件清单"表，明确双文件分工（发版前写 CHANGELOG.md 覆盖旧版、追加 CHANGELOG-ALL.md 顶部）。
- `release-gh.sh` 的 `NOTES_FILE` 仍指向 `CHANGELOG.md`（Release 说明 = 当版，符合预期）。三文件均未提交。

## 设置页"辅助功能"按钮修复：去掉强制授权弹窗（14:31）
- **根因**：用户点设置页"一键添加辅助功能权限…"按钮时弹出系统级授权弹窗（"AppLaunchpad 想要使用辅助功能来控制这台电脑"，带「打开系统设置」/「拒绝」按钮），用户问"为什么不能直接打开设置页"。原代码 `requestAccessibility()`：
  ```swift
  let opts = ["AXTrustedCheckOptionPrompt": true] as CFDictionary
  AXIsProcessTrustedWithOptions(opts)  // ← 这行就是弹窗的元凶
  NSWorkspace.shared.open(url)
  ```
- **macOS TCC 实际行为**（关键，避免后续 AI 又走老路）：① App 启动后只要**实际调用了**需要辅助功能的 API（如 `NSEvent.addGlobalMonitorForEvents`，本 App 已在 `AppDelegate.swift:112` 用上），系统会**自动把 App 登记到设置页"辅助功能"列表里**，不需要任何弹窗；② `AXIsProcessTrustedWithOptions(prompt: true)` 只是**显式"申请"**触发系统弹窗，并不会让 App 进列表（App 早已在列表里）。所以既然 App 已经在跑且用了 addGlobalMonitorForEvents，**列表里一定有 AppLaunchpad，直接打开设置页就能让用户勾选**，完全不需要那个询问框。
- **修复**（`InteractionPane.swift`）：① 删除 `AXIsProcessTrustedWithOptions(opts)` 整行及已无人调用的 `requestAccessibility()` 函数；② 按钮内联只保留 `NSWorkspace.shared.open("x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility")`；③ 按钮文字 "一键添加辅助功能权限…" → "打开辅助功能设置…"（更准确）；④ 警告文本加一句"如在列表中找不到 AppLaunchpad，请先关闭本程序再重新启动一次即可出现"，应对冷启动未及 addGlobalMonitorForEvents 的极端情况。
- **编译**：`xcodebuild ... clean build` → **BUILD SUCCEEDED**。改动未提交。

## Photos 等第一方 App 英文显示 bug 修复（Plan A 落地）
- 用户问：LSCopyApplicationName 私有 API 调用会慢吗？结论：不慢（LS 内存缓存亚毫秒级），故直接调 + 按 path memo 化（OSAllocatedUnfairLock）。
- 实测发现（macOS 26 SDK + 容器）：
  1) LSCopyApplicationName 经 dlsym 全局符号表**解析不到**（符号已移除/改名）→ 改用 dlopen(nil,0)+dlsym 动态解析、nil 安全降级。
  2) NSURLLocalizedNameKey / LSCopyDisplayNameForURL 即使强制 -AppleLanguages (zh-Hans) 仍返回 "Photos" → 仅文件级本地化，取不到系统级"照片"，不可用。
- 改 AppScanner.swift：localizedDisplayName 回退到 baseDisplayName（bundle 无本地化）时，再调 systemDisplayName(for:)（LSCopyApplicationName 兜底），结果 memo 化。makeAppInfo 用 finalDisplayName 构造 AppInfo。
- 编译 BUILD SUCCEEDED。
- 待用户在其中文 Mac 实测：若 Photos 仍英文，说明其机器符号也不在，需改用硬编码第一方 bundle id→中文名映射表作为退路。

## Photos 等第一方 App 中文显示 bug 修复（最终落地：Spotlight 公开 API）
- 用户实测确认 LSCopyApplicationName 在其 macOS 26 上解析不到（符号彻底移除）→ Plan A 原样不可用。
- 研究 references/LaunchNext/AppInfo.swift 的 `bundlePreferredDisplayName`（读 bundle 自身 lproj），结论：与本项目原 `localizedDisplayName` 思路一致，对 Photos 同样拿不到"照片"（Photos 的 zh_CN.lproj 为空），**治不了此 bug**。
- 真因「照片」来自系统级 App 名数据库；Spotlight 的 `kMDItemDisplayName`（=`NSMetadataItemDisplayNameKey`，公开 API，`import CoreServices`）走同一数据库，返回"照片/音乐/图书/播客/无边记/计算器/日历"等，第三方 Xcode 返回原名。实测 200 次 ~90ms（<0.5ms/次）。
- 落地：AppScanner.swift 删除 dlopen/dlsym/unsafeBitCast（LSCopyApplicationName）整段 hack，改为 `NSMetadataItem(url:).value(forAttribute: NSMetadataItemDisplayNameKey)`；保留 path memo 缓存（OSAllocatedUnfairLock）。imports：Darwin→CoreServices。编译 BUILD SUCCEEDED。
- 调用点不变：`makeAppInfo` 中当 `displayName == baseDisplayName`（bundle 无本地化）时才调 `systemDisplayName(for:)`，命中则用系统中文名。

## 搜索框浅色模式可见性 + 液态玻璃化（方案 A）
- 浅色模式对比度问题：SearchBarView 全部写死 .white 导致浅色磨砂背景上看不清。改为语义色 .primary/.secondary（自适应浅/深色），BUILD SUCCEEDED。
- 用户选方案 A：搜索框背景由 .ultraThinMaterial 改为 GlassHostingView(cornerRadius:12) 液态玻璃，与面板默认玻璃背景统一、.regular 玻璃实体感更强对比度更好。BUILD SUCCEEDED。
- ⚠️ 焦点风险（待真机验证）：GlassHostingView 把内容塞进独立 NSHostingView 且不转发父视图环境，跨 hosting 的 @FocusState 协调可能失效（影响 ESC 关台/方向键翻页/输入）。LaunchpadView 持 @FocusState searchFocused 经绑定注入 SearchBarView.textField，onChange(isVisible) 做 false→async true 强制聚焦。若真机焦点异常，SearchBarView 注释已写明回退到 .background(.ultraThinMaterial, in: RoundedRectangle(cornerRadius:12))（方案 B）。

## 包裹型/非标 app 扫描缺失修复（安居客）
- 用户反馈 /Applications/安居客.app 在启动台搜不到。根因：外层 .app 无 Contents/Info.plist（makeAppInfo 首道门槛 return nil 被丢弃）；真实包在内层 Wrapper/Anjuke.app，且 Info.plist 裸在顶层（非 Contents/），外层用 WrappedBundle 软链指内层。系统启动台能显示是因 LS 顺软链解析。
- 修复：AppScanner 新增 resolveBundle(at:depth:)，解析顺序：①Contents/Info.plist ②顶层 Info.plist ③WrappedBundle 软链内层 ④Wrapper/*.app；递归深度<4 防 WrappedBundle 自指死循环。bundleURL 读元数据/资源，launchURL 始终用最外层 url（与 Finder/系统启动台一致，由系统走 wrapper 启动）。显示名取内层 CFBundleDisplayName="安居客"，isMASApp 同时查 Contents/_MASReceipt 与顶层 _MASReceipt。编译 BUILD SUCCEEDED；独立 Swift 验证：bundleID=com.anjuke.anjuke、displayName=安居客、launchURL=外层。
- 注意：扫描枚举器 .skipsPackageDescendants 把外层 .app 当 package 跳过递归，故内层不会被重复枚举，无重复风险。

## 包裹型 fix 的回归 bug（plist 路径误改为顶层）
- 用户反馈：fix 后只剩 7 个包裹型 app，所有正常 app 都没了。
- 根因：resolveBundle 对正常 app 返回 (url,url) 正确，但 makeAppInfo 改成 `plistURL = bundleURL.appendingPathComponent("Info.plist")`（顶层），而正常 app 的 Info.plist 在 Contents/Info.plist，导致 NSDictionary 读不到、guard 失败、整个 makeAppInfo return nil → 正常 app 全部漏扫。
- 修复：让 resolveBundle 直接返回真正找到的 plistURL（与 bundleURL/launchURL 一起），makeAppInfo 用它直接读 Info.plist，不再在调用方自己拼路径。正常走 Contents/Info.plist，非标走顶层 Info.plist，包裹型走内层 plist。BUILD SUCCEEDED；测试 6 个 app（包裹+正常）全部解析正确。
- 教训：返回 bundleURL 让调用方自取 plist 时容易按"标准路径"假设拼错；应让解析函数一并返回"具体文件路径"以避免此类拼路径 bug。

## 研究 LaunchNext 的「导入原生启动台布局」实现 + 修正 Tahoe 误判
- 用户问 LaunchNext 如何实现导入原生布局。读 `references/LaunchNext/LaunchNext/NativeLaunchpadImporter.swift` 完整实现：DB 路径=`/private$(getconf DARWIN_USER_DIR)com.apple.dock.launchpad/db/db`，sqlite3 只读解析 apps/groups/items 三表，type 1=root/2=page/3=folder/4=app，按 parent_id+ordering 重建层级。
- **重大更正**：之前以为 Tahoe 移除了 Launchpad DB（查 `~/Library/Application Support/Dock/` 不存在）→ 错！实测 `getconf DARWIN_USER_DIR` 拼出的路径在 macOS 26.5.1 真实存在（8.5MB，legacy 三表齐全、含中文标题"音乐/Safari浏览器"）。自动导入当前系统布局完全可行。已修正 MEMORY.md 对应章节。
- 本项目可行映射：cell 序列（app+folder）按本机 itemsPerPage 重切页、folder→FolderInfo、未安装 bundleID 跳过；导入前备份 layout.json。bundleID 匹配用本机已扫描 allApps。
EOF\necho "appended"
- 设计文档已落地：`docs/技术实现/07-导入原生启动台布局设计.md`（策略 A 保序重流、SQLite3 解析、设置页按钮、backup、测试清单）。用户选「先给详细设计文档」暂不动代码。

## 导入原生启动台布局：从设计稿落地为代码（16:14）
- 用户要求实现该功能，并补充：①需要开关（导入原生布局）；②能否恢复默认布局；③导入出问题不能影响现有功能；④细节补进文档。
- 实现（xcodegen generate 后 xcodebuild → BUILD SUCCEEDED）：
  - `NativeLaunchpadImporter.swift`：系统 `import SQLite3`（动态链 `/usr/lib/libsqlite3.dylib`，app 体积零增长）只读解析原生 db → `[NativeCell]`（保序、文件夹重建、占位名兜底前 3 个 app 名），异常向上抛；`NativeCell`/`NativeImportError` 标 `Sendable`。
  - `LayoutStore.backup()`：layout.json → layout.backup.json。
  - `LayoutService.importNativeLayout()/restoreDefaultLayout()` + `NativeImportResult`：后台 `Task.detached` 解析、主线程映射；失败**绝不写 data.layout**，仅返回错误——现有功能不受影响。
  - `LaunchpadViewModel` 薄壳转发；`UserPreferences.useNativeLayout` 开关（持久化）；`AppContext.viewModel` 打通设置页→导入逻辑；`AppDelegate` 启动写入。
  - `AppearancePane`「布局」Section：Toggle「使用原生启动台布局」+「导入系统启动台布局」+「恢复默认布局」按钮 + 确认 alert + 结果文案。开关=状态标记，不自动重导（避免覆盖手动调整）；恢复默认用 `LayoutData.initial`。
- 文档同步更新 `docs/技术实现/07-导入原生启动台布局设计.md`：状态改"已实现"、§5 UI 落地、§10 开关/恢复默认/安全保障/体积零增长。
- 未提交（用户未要求 commit；此前还有 33b1477/0e8312a 两提交及安居客修复未推送）。

## 导入原生布局补强：本机已装但原生库未收录 App 不丢失（17:04）
- 用户担心：后续安装的 App 可能不在原生 macos 启动台数据库里，导入后消失。
- 事实：原生 db 由系统启动台维护，通常包含后续安装 App；但存在例外（包裹型 App、特殊路径、数据库滞后），故「原生库 App 集合 ⊆ 本机已装 App 集合」不恒成立。
- 补强 `LayoutService.importNativeLayout()`：保序重流生成 pages+folders 后，计算已覆盖 App（含文件夹内），把 `data.allApps` 中未被覆盖的 App 依次**追加到末页**（满页新建页），计为 `appended`；`NativeImportResult.success` 新增 `appended` 关联值；`restoreDefaultLayout` 补 `appended:0`。设置页文案：成功时显示「追加 N 个本机已装但启动台未收录的 App」。
- 结论：导入绝不会让本机已装 App 消失；导入后新装 App 由 `refreshApps()`→`mergeLayout()` 自动追加末页（FSEvents 触发），无需重导。
- 文档 §10.6 补该处理逻辑；BUILD SUCCEEDED（编译通过）。仍未提交。

## 布局 Section UI 改造：两个互斥胶囊开关（17:49）
- 用户反馈原 UI（1 个 Toggle + 2 个按钮）含义混乱，建议改为两互斥胶囊开关更直观。AskUserQuestion 确认默认选中→用户选「默认开『原生 macOS 启动台布局』」。
- 改造 `AppearancePane.swift`：删「导入/恢复」两个按钮，原 Toggle 改为「使用原生 macOS 启动台布局」+ 新增「使用本项目默认布局」两胶囊 Switch；互斥在 `Binding.set` 里手动维持：开 A 弹导入确认，开 B 弹恢复确认；取消/失败时 `useNativeLayout` 不被写。第一个 Switch 在 `!dbAvailable` 时 `.disabled`。
- `UserPreferences.init()`：`useNativeLayout` 默认值由「key 不存在即 false」改为「key 不存在 → `NativeLaunchpadImporter.hasImportableDatabase() ? true : false`」；已存用户偏好不受影响。
- 文档 §10.1/10.2/10.5 同步更新；BUILD SUCCEEDED。仍未提交。