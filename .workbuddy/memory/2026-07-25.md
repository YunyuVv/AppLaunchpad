# 2026-07-25 工作日志

### 公众号内容规划（15:30）
- 为公众号「不加糖的猫」（catcodingai）规划了 **AI Coding 实践系列**，共 6 篇文章
- 基于 AppLaunchpad 项目的真实开发经历；**15:50 已迁移至 Obsidian**：`/Obsidian/自媒体/公众号内容管理/文章/开发实战/AI Coding 实践系列/`（原 `docs/公众号内容规划/` 已删除）
- 包含：总策略 README + 6 篇详细大纲（标题候选、结构、素材来源、配图清单、写作注意事项）
- 系列：引爆篇 → 方法论(上/下) → 踩坑录 → 深度技术 → 思考篇
- 内容配比遵循 60/30/10 法则（干货/故事/引导）
- **15:39 起写完整正文**：已产出 `草稿-01-引爆篇.md`（可直接发布版，含截图占位符），并用 @resvg/resvg-js 生成两张配图 PNG（`assets/chart-dev-stats.png` 开发统计、`assets/timeline-phases.png` 阶段时间线）+ 同款 SVG 源文件。产品运行截图需用户自行补（app 真实界面无法生成）。发布工具建议 md2weixin/墨滴，封面用 `logo/applaunchpad-icon-glass-512.png` 做主视觉。

- **App 图标操作能力需求文档（00:10 用户要求）**：用户问「macos 原生启动台 右边app 有什么操作 写一份需求文档 本项目也要支持上」。解读「右边app」= app 图标的右键上下文菜单 + 全部 app 级操作。先排查现状：单击启动(N1✅ AppIconView.onTap→launch)、长按编辑模式(N2✅ onLongPress 0.5s→enterEditMode)、拖拽重排(N3✅ make-way)、主网格文件夹建/归入(N4/N5❌ 运行期已移除)、删除X(N6△ 仅文件夹展开视图有 onDelete，主网格无且未区分商店)、右键菜单三件套(N7/N8/N9❌ 当前 .contextMenu 仅背景层「关闭启动台」)、拖到Dock(N10❌)。产出 `docs/需求/App图标操作能力需求.md`：原生全集(N1–N11)+现状对照矩阵+需求 R1(右键菜单三件套 P0)/R2(在Finder显示 P0)/R3(添加到程序坞 P0)/R4(主网格删除X+仅AppStore可见 P1,复用 AppInfo.isMASApp)/R5(拖到Dock P2)/R6(恢复文件夹 P2)。关键依赖：AppScanner 已采集 `AppInfo.isMASApp`(经 _MASReceipt 判定, AppScanner.swift:78)，正好用于原生「仅商店app显X」。实现要点：AppIconView 加 .contextMenu 不冲突现有 onLongPressGesture；右键恒弹菜单≠进编辑；添加到程序坞走 Dock plist + killall Dock(后台Task)。
- **Bundle ID 改名的代价分析（00:05 用户问 com.applaunchpad.app → com.biliww.applaunchpad 能否改）**：结论能改(改动仅 project.yml 2行+Info.plist 1行, pbxproj 由 xcodegen 重生)，但等于"换全新app"有3代价：①TCC辅助功能授权失效需重授(`tccutil reset Accessibility com.applaunchpad.app`清旧)、②UserDefaults.standard 域随bundleID变→所有设置清零(trackpadPagingGain/外观/快捷键)、③Manual签名需 com.biliww.applaunchpad 已在团队 2VU69Q9CGK 注册否则编译失败。布局 layout.json 目录硬编码"AppLaunchpad"不受影响、entitlements/sandbox/keychain 无关。用户未决定改。

- **folder 拖出 Bug 1「两个一样的 app」+ Bug 2「最后一排看不见」(00:18 用户反馈)**：
  分析代码前先看 `~/Library/Application Support/AppLaunchpad/layout.json` 铁证——pages[0] 53 项 / pages[2] 54 项里有 12 个跨页重复 (BlueStacks/Taobao/GeminiMacOS/FindMy/Clock/Baidu/Doubao/FaceTime/ColorSyncUtility/ClaudeForDesktop/BootCampAssistant/Chrome.app.mnbdejlhejaiignlpmdnlnpedpmlfodm) + pages[0] 内部 49-50 紧邻重复。所有 12 个重复 app 都不在 `folders.appIDs` 里，与 folder 拖出逻辑直接相关。

  **Bug 1 根因**：`DragController.endDrag` 走分支 3 跨页插入时**不查重**——目标页若早已有同名 .app（之前 folder 拖出 / 历史 bug 留下的副本），`dstItems.insert` 直接插，**重复两份**。layout.json 里 12 个跨页重复都是这条路径历史产物。从 folder 拖出 app 时，folder 缩略图关闭后，原本隐藏在 folder 里的副本 + pages 里的重复份同时显示为独立 app 图标 → 视觉"两个一样的 app"。

  **Bug 2 根因**：`endDrag` 的 `effectiveDst = min(to, dstItems.count)` 在 pages **不满**时 clamp 到 `count-1`，但 `geo.slotUnderCursor` clamp 到 `itemsPerPage-1`（视觉最后一格）。**视觉落点 = itemsPerPage-1，实际插入 = count-1**（倒数第二排末尾） → app 真的在 pages 里，但在视觉预期位置的左上方，肉眼看不到；翻页回来位置不动，反而看得到。

  **修法（一次到位）**：
  ① 清理 layout.json：写 `/tmp/dedupe_layout_v2.py` 全局去重 pages 里的同名 .app（保留全局第一次出现位置），备份到 layout.json.bak，**共清掉 13 个重复**。
  ② `DragController.endDrag` 走分支 3：`effectiveDst = min(max(to, 0), layoutService.itemsPerPage - 1)` 与 `pageItemsWithDrag` 视觉占位完全一致；`sourcePage != dstPage` 时先 `dstItems.removeAll` 同名 .app（只在跨页去重——同页 src 已在 srcItems.remove 移走，避免重复 remove）。
  ③ `DragController.pageItemsWithDrag` 排序模式 + 翻页后目标页插入占位两处：`min(max(to, 0), layoutService.itemsPerPage - 1)` 与 ② 保持一致。
  BUILD SUCCEEDED（清 DerivedData+禁用沙箱全量重编）+ app 重启。

  **红线**：`endDrag` clamp 一律用 `itemsPerPage - 1`，**绝不**用 `dstItems.count`——否则不满页时视觉/实际落点偏移。跨页移动时**必须**对 dstPage 查重去重。layout.json 是用户实际状态的真实记录，**遇"重复"类 bug 必先 `python -m json.tool` + 全文搜索 bundleID**。

- **拖拽崩溃 Bug 3「pageItemsWithDrag Array out of range」(01:19 用户反馈截图 PID 56719)**：
  **崩溃栈**：`DragController.pageItemsWithDrag(pageIndex: 2)` line 321 `items.insert(item, at: min(max(to, 0), itemsPerPage - 1))`，`src=31, to=48, items=40, item=unimplemented enum kind` (Swift/Array.swift:430 Fatal error)。`item=unimplemented` 是 LLDB 越界时 Array 内部 buffer 被破坏的副作用，不是根因。

  **真根因**：上次 fix（folder 拖出 bug 修复时）把 clamp 改成 `itemsPerPage - 1` 是为了满页"视觉最后一格 = itemsPerPage-1"语义，但**没考虑不满页**：满页时 `itemsPerPage - 1 = count - 1` 没事；**不满页时 `itemsPerPage - 1 >> count - 1` → `items.insert(at: 越界值)` 崩溃**。崩溃场景：用户拖拽到第3页 pageIndex=2 (items=41 含 4 个 .folder + 37 个 .app) 槽位 48 (7×7 网格最后一格)，remove src=31 后 count=40，clamp to 48 越界。

  **深层污染**：「彻底移除文件夹」实际未完成——
  - `FolderController.swift` / `Views/FolderThumbnailView.swift` / `Views/FolderExpandedView.swift` 三个文件**都还在**
  - `LayoutService.mergeLayout` 仍保留 .folder 槽位在 pages（`page.filter { if case .app... return ...; return true }` 保留 .folder，再 `validFolderIDs` 仅清"空 folder"）
  - `DragController` 仍引用 `folderController`
  - `LaunchpadView` 仍有 `expandedFolderID` / 文件夹面板 overlay / 4 处 `onTapFolder` / floatingDragIcon folder 分支
  - layout.json 含 **12 个 .folder 散落 pages** + `folders` 字段是 `[key, dict, key, dict]` 异常格式（Swift JSONDecoder 容错解码为 dict 成功 → validFolderIDs 含全部 12 个 → 全部保留）

  **P0 + P1 全做（一次到位）**：
  ① **pageItemsWithDrag line 321 + endDrag line 258 clamp 改成 `items.count`（=pageItems.remove 后的 count）**：满页时=itemsPerPage-1=视觉最后一格；不满页时=末尾 append（`insert(at: count)` = append 合法）。统一两个场景，**永不越界**。修复 P0 即解决"卡死退出"。
  ② 删 `FolderController.swift` / `FolderThumbnailView.swift` / `FolderExpandedView.swift` 三个文件。
  ③ `LaunchpadViewModel`：删 `folderController` 属性 + `init` 中第 3 个参数 + 5 个 folder 转发方法（folderInfo/createFolder/deleteFolder/renameFolder/folderAtIconPoint）+ `beginDrag(folderID:)`。
  ④ `LaunchpadData` 删 `folderAtIconPoint` 方法。
  ⑤ `LaunchpadView`：删 `expandedFolderID` 状态 + `dragStart.folderID` 元组字段 + 文件夹面板 overlay 整个 if 块 + 起点 folder 起手分支 + `pagingDragGesture` 注释里的 folder 提及 + 4 处 `onTapFolder: { _ in }` + floatingDragIcon 的 folder 分支。
  ⑥ `GridPageView`：删 `onTapFolder: (UUID) -> Void` 参数 + 把 `case .folder(let folderID):` 改为简单 `case .folder: Color.clear` 兜底（LayoutItem.folder 保留作为数据兼容 enum case）。
  ⑦ `DragController`：删 `folderController` 属性 + `init` 第 3 个参数 + `beginDrag(folderID:)` 方法 + endDrag 的"创建文件夹"+"添加到已有文件夹"两个分支 + `detectHoverTarget`/`clearHover` 方法 + placeholder 的 `.folder` 分支。删 `removeFromPage`/`findInPage` 私有方法（无人调用）。
  ⑧ `LayoutService.mergeLayout`：用 `expandFolders` 函数把 pages 里的 .folder 槽位按 `saved.folders` 字典展开为内部 .app（按 appIDs 顺序，已卸载的静默丢弃），**return 强制 `folders: [:]`**（仅作兼容字段保留在 LayoutData，不参与运行时）。
  ⑨ 写 Python 迁移脚本清理 layout.json：备份 `.folderclean.bak` → 解析 folders 字段（支持 list-of-pairs 异常格式）→ 12 个 .folder 全部展开为 58 个 .app（157→215 个）→ folders 强制空 dict 写入。从 4 页 169 项 变成 4 页 215 项（.app 215 个 + 0 .folder）。
  ⑩ 留 `DragState.hoverTargetBundleID/FolderID/hoverTargetSlot/draggedFolderID/draggedItemType` 字段保留（GridPageView 还读 `hoverTargetBundleID` 用于 scaleEffect 1.15；但 DragController 不再写它所以恒为 nil，不影响功能）。
  BUILD SUCCEEDED（清 DerivedData+xcodegen+禁用沙箱全量重编）+ app 5秒存活验证。

  **红线更新**：
  ① `pageItemsWithDrag` / `endDrag` clamp 一律用 **`items.count`/`dstItems.count`**（=remove 后的 count），**绝不**用 `itemsPerPage - 1`——满页时虽然结果一样，但不满页时 `itemsPerPage - 1` 远大于 count 导致越界。`min(max(to, 0), items.count)` 覆盖满/不满两种场景，to 越界时等价 append。
  ② 「彻底移除 X」类重构**必须先删文件+改 project.yml 引用+xcodegen generate**，否则旧文件仍在导致新逻辑分支跳不过。
  ③ 任何 enum case 删除前**保留** enum 本身和默认值（`case .folder: Color.clear`）作为兼容兜底——这样历史 layout.json 不会让 switch 编译失败。
  ④ 用户截图 PID 56719 实际**未真崩**——Xcode debug session 状态 ≠ app 进程退出，验证 app 健康应看 `kill -0 $PID`（PID 56719 一直存活）。

- **拖完 app 后无法翻页（需点一下空白才行）(01:26 用户反馈)**：
  **根因**：`globalDragGesture.updating` 拖拽起点调用 `enterEditMode()`，但 `endDrag()` 只重置 `dragState`、**从不退出编辑模式** → `isEditMode` 持续 true。而 `pagingDragGesture` 的 onChanged/onEnded 守卫都有 `!viewModel.isEditMode` 条件 → 编辑模式下翻页手势整体被 `return` 屏蔽。用户点一下空白触发 `Color.clear.onTapGesture → exitEditMode()`，`isEditMode` 变 false 才恢复 → 正好对应"点一下才能翻"。与之前 07-23「松手后无法点击」(`isDragging` 卡死) 是**同源但不同**的两道独立 guard。
  **修法**：移除 `pagingDragGesture` 两处守卫里的 `!viewModel.isEditMode`，让**编辑模式下也能翻页**（拖空白翻页 / 拖边缘自动翻页均可用），贴合原生 macOS 启动台（抖动/X 只是视觉态，不该阻断翻页；长按删 app 时也能翻页找其他页）。
  **红线**：「翻页/拖拽结束后的可交互性」由 `dragState.isDragging`（07-23 修）和 `isEditMode`（本次修）两道独立 guard 控制，二者不可混为一谈；`isEditMode` 只应影响视觉（抖动/X），**绝不可屏蔽翻页手势**。
  BUILD SUCCEEDED + app 重启验证（PID 69315 存活）。

- **恢复文件夹功能（01:31 用户反馈"无法创建文件夹，悬浮不到 B 上方"）**：
  之前清文件夹代码时把 `detectHoverTarget`/`clearHover` + endDrag 的创建文件夹/合并到已有文件夹分支 + 3 个文件夹视图文件全删了，拖拽 app A 到 B 上无悬浮检测→无触发时机。
  **修法**：从 git HEAD（`2cdbfb2` 文件夹拖拽去重修复版）恢复全部文件夹代码：
  ① 恢复 3 个被删文件：`FolderController.swift`、`FolderThumbnailView.swift`、`FolderExpandedView.swift`
  ② `DragController.swift`：用 HEAD 版本 + 重做 P0 clamp 修复（`items.count`/`dstItems.count` 替代 `itemsPerPage-1`）
  ③ `LaunchpadViewModel.swift`：恢复 `folderController` 属性、init 参数、5 个转发方法 + `beginDrag(folderID:)`
  ④ `LaunchpadData.swift`：恢复 `folderAtIconPoint` 方法
  ⑤ `LaunchpadView.swift`：HEAD 版本恢复 + 重做 BUG「编辑模式屏蔽翻页」修复（移除翻页守卫 `!isEditMode`）
  ⑥ `GridPageView.swift`：恢复 `onTapFolder` 参数 + `case .folder` 渲染逻辑
  ⑦ `LayoutService.mergeLayout`：恢复保留 `.folder` + `folders` 字典（不展开）
  layout.json 的 folders 字段已是空 dict（之前清过），运行时从零开始创建新文件夹。
  xcodegen + BUILD SUCCEEDED + 重启验证（PID 69366 存活）。

- **App 显示名本地化 Bug「wechatwebdevtools 显示英文"wechatwebdevtools"而非中文"微信开发者工具"」(用户误以为它是微信，搜 wechat 出现第二个 app)**：
  **本质**：系统里**两个独立 app**——`WeChat.app`(bundleID `com.tencent.xinWeChat`, 显示名"WeChat") 和 `wechatwebdevtools.app`(bundleID `com.tencent.webplusdevtools`, 微信开发者工具)。后者图标也不同(Finder 里绿底单气泡+箭头 vs 微信双气泡)。用户以为第二个"应该是微信"，实际是不相干的 app。它的 zh_CN.lproj/InfoPlist.strings 里明写 `CFBundleDisplayName = "微信开发者工具"`，AppScanner 以前只读 Info.plist 字面量漏掉了本地化。
  **Foundation 大坑**：原想用 `Bundle.localizedString(forKey:"CFBundleDisplayName", table:"InfoPlist")` / `Bundle.localizedInfoDictionary`，但 `wechatwebdevtools.app` 的 `CFBundleDevelopmentRegion="en"` 且**无** `CFBundleLocalizations` 字段 → 实测 `Bundle.preferredLocalizations` 返回 `["en"]` 而非 `["zh_CN"]` → Foundation 错选 en，只读到 en.lproj/InfoPlist.strings 的 "Wechat Devtools"。Spotlight(mdls kMDItemDisplayName) 在同样输入下正确返回"微信开发者工具"，证明系统级 API 走不同路径。
  **修法（AppScanner.swift）**：新增 private static `localizedDisplayName(resourcesURL:, developmentRegion:, fallback:)` 手写匹配——列 `Contents/Resources/*.lproj` 目录，按 `Locale.preferredLanguages` 顺序生成候选目录名（用户语言"zh-Hans-CN"→转下划线"zh_Hans_CN"→ split→ 取 `parts.first="zh"` 为 lang、`parts.last="CN"` 为 region，**务必取 last 不是 parts[1]**，否则"Hans"被当地区→生成"zh_Hans.lproj"匹配失败）；候选顺序 `zh-Hans-CN.lproj` → `zh_Hans_CN.lproj` → `zh_CN.lproj` → `zh.lproj` → 兜底 `developmentRegion.lproj`；命中后读该 `lproj/InfoPlist.strings` 的 CFBundleDisplayName。**绝不可用 `Bundle.localizedString`/`localizedInfoDictionary` 走这个 app**。
  **验证**：swiftc 独立编译复制逻辑跑 6 个 app——WeChat→"微信"、wechatwebdevtools→"微信开发者工具"、Safari→"Safari浏览器"、Mail/Preview/Terminal→保持英文(这些 app 本身无中文 lproj 覆盖, 行为同系统)。✓ 逻辑正确。**注意**：swift-plugin-server 工具链仍坏(xcodebuild 阻塞)，本次仅逻辑验证，待重启 Mac/修 Xcode 后全量构建。

- **键盘左右箭头无法翻页 Bug「搜索框选中后每次打开都残留焦点，吞掉左右箭头」**：
  **根因**：`LaunchpadWindowController.setupKeyMonitor` 里有 `if self.isTextInputFirstResponder() { return event }`——**无条件**把所有按键交还输入框。而 `panel`/`NSHostingView` 在 `show()` 时**复用不重建**→ `SearchBarView` 的 `@FocusState private var isFocused` 跨 `hide()/show()` 保留；`hide()` 只清 `searchText=""` 但**没让搜索框失焦**→ 重新 `show()` 后 field editor 仍是 firstResponder → `isTextInputFirstResponder()` 一直 true → 左右箭头（case 123/124）被直接 return 吞掉，永远走不到 `goToPreviousPage/NextPage`。`isSearching` 本身随 searchText 同步（清空即 false），所以不是搜索态卡住，而是**焦点残留导致的按键吞噬**。
  **优化方案对比**：①(最小) 放行条件加 `!viewModel.searchText.isEmpty`——搜索框空(残留焦点)时左右箭头走翻页，功能即恢复；②(治本) 上提 `@FocusState` 到 LaunchpadView，每次 `isVisible→true` 强制 `searchFocused=false`，连视觉残留(光标闪烁)也清理；③(组合,采用) ①②一起。还权衡过：不放行则文件夹重命名等空输入框会冲突，但 AppLaunchpad 实际**只有一个** TextField(搜索框)，无重命名内联框，故安全。
  **修法**：① `LaunchpadWindowController.swift` `setupKeyMonitor` 放行改为 `if self.isTextInputFirstResponder(), !viewModel.searchText.isEmpty { return event }`；② `SearchBarView.swift` 去掉内部 `@FocusState`，改为接收父视图注入的 `var focus: FocusState<Bool>.Binding`（`.focused(focus)`）；③ `LaunchpadView.swift` 加 `@FocusState private var searchFocused: Bool`，`SearchBarView(... focus: $searchFocused)` 调用处加 `.onChange(of: viewModel.isVisible) { if visible { searchFocused = false } }`。
  **验证**：改动为标准 SwiftUI 用法、类型自洽；但 swift-plugin-server 工具链仍坏 + 用户拒清理 DerivedData 的构建命令 → 未走 xcodebuild。待重启 Mac/修 Xcode 后全量构建验证。

- **拼音输入分裂 Bug「打 haima 拼音，h 进一处、aima 进另一处」(上一轮「失焦修复」引入的回归)**：
  **根因**：上轮为修「箭头被吞」在 `LaunchpadView` `show()` 时强制 `searchFocused = false`，导致**搜索框永远不自动聚焦**。于是打开启动台打拼音时：首字母 `h` 因搜索框无焦点 → 被键盘 monitor `default` 分支直接 `searchText += "h"`（绕开输入框）；后段 `aima` 由系统拼音 IME 在发现窗口有 TextField 后写回输入框本身。两套写入机制同时作用于 `searchText` → 分裂（"h 一个、aima 一个"）。
  **修法（组合，既要 IME 不分裂、又要翻页不被吞）**：
  ① `LaunchpadView.swift` `show()` 聚焦：`onChange(of: viewModel.isVisible) { if visible { searchFocused = false; DispatchQueue.main.async { searchFocused = true } } }`——先 false 再 async true，确保 panel/NSHostingView 复用（不重建）时 SwiftUI @FocusState 能真正重新聚焦（直接 true 因值未变可能不 re-apply）。
  ② `LaunchpadWindowController.swift` `setupKeyMonitor` 重写焦点门控：
    ```
    let focused = self.isTextInputFirstResponder()
    let isPrintable = (event.characters?.isEmpty == false) && !cmd/ctrl/opt
    if focused {
        if viewModel.searchText.isEmpty {
            if isPrintable { return event }   // 聚焦且空：可打印字符交还输入框，让 IME 从首字母起在 firstResponder 上下文合成
        } else {
            return event                       // 聚焦且有内容：全部交还输入框（光标/删除/IME）
        }
    }
    // 未聚焦：走下方字母搜索/导航
    ```
  **关键不变量**：搜索框**必须在 show 时聚焦**（与原生 Launchpad 一致），否则拼音 IME 必分裂；monitor 在「聚焦且空」时**可打印字符须交还输入框（return event）而非自行 `searchText +=`**，否则首字母被截走、其余被 IME 写回 → 分裂。方向键/ESC 仍落入下方导航逻辑 → 翻页不被吞（上轮箭头 bug 仍有保障）。
  **待验证**：swift-plugin-server 工具链仍坏 → 未走 xcodebuild，改动为标准 SwiftUI 用法、逻辑已逐场景核对。待重启 Mac/修 Xcode 后全量构建。

- **键盘方向键行为「待决策」已归档到文档（代码现状不动）**：
  用户确认 macOS 原生启动台四方向键只做页内高亮移动、翻页仅触控板/点圆点、不支持键盘翻页；而 AppLaunchpad 当前 `LaunchpadWindowController.setupKeyMonitor` 把左右改成了翻页、上下改成了选择（偏离原生）。已建 `docs/后续优化功能.md` 记录原生行为 / 现状 / 三选项（A 保持现状、B 对齐原生、C 现状+设置开关）+ 代办清单。**当前代码保持不变**，后续优化时再按用户拍板的方向改。

- **App 图标操作：右键菜单落地（替代编辑模式 X）**：
  用户要求「删除」放右键菜单、动作与原生一致（把 `.app` 移入废纸篓），且**不要编辑模式 X**（不好看）。已实现：
  - `AppIconView`：移除原 `onDelete` 参数与「编辑模式 X 按钮」；新增 `onDeleteApp` 参数 + `.contextMenu`：①打开(`NSWorkspace.open(url)`) ②在访达中显示(`activateFileViewerSelecting([url])`) ③删除(`onDeleteApp?()`)。`/System/` 下受保护 app 不显示「删除」。
  - `LaunchpadViewModel.deleteApp(_:)`：主线程 `Task` 内 `FileManager.trashItem(at:)` 移入废纸篓（失败静默），随后 `await layoutService.refreshApps()`；`mergeLayout` 会自动剔除已卸载 app。
  - `GridPageView` 主网格 app 格传 `onDeleteApp: { viewModel.deleteApp(app) }`；`FolderExpandedView`(2处) / `LaunchpadView` 拖拽浮动图标 的 `AppIconView` 调用去掉已删除的 `onDelete`（默认 nil）。`FolderThumbnailView` 的文件夹 X 删除保持原样（用户未要求改）。
  - 原生右键仅靠 secondary-click 触发，与 primary 长按进编辑模式互不冲突。
  **注意**：swift-plugin-server 工具链仍坏 → 未走 xcodebuild；改动为标准 SwiftUI 用法、已逐文件核对引用完整性。待重启 Mac/修 Xcode 后全量构建验证。

- **删除加二次确认（2026-07-25）**：应要求，`AppIconView` 右键「删除」项不再直接调 `onDeleteApp?()`，改设 `@State showDeleteConfirm` → 弹 `.confirmationDialog("确定把\"<displayName>\"移到废纸篓？", ...)`，确认按钮（`role: .destructive`）才执行 `onDeleteApp?()`，取消按钮 `role: .cancel`。弹窗含正文「此操作会将应用移入废纸篓，可在废纸篓中恢复。」。系统受保护 app（/System 下）仍不显示「删除」。改动为纯 SwiftUI 用法、未走 xcodebuild（工具链仍坏）。

- **删除后无「放回原处」(Put Back) 根因 + 修复（2026-07-25）**：用户反馈 AppLaunchpad 删到废纸篓的 app 右键没有「放回原处」，而 Finder 拖删的有。根因：`deleteApp` 原用 `FileManager.default.trashItem(at:)`（Foundation 底层 API），只把文件挪到 ~/.Trash，**不写原始路径元数据** → Put Back 不可用。修复：改用 `NSWorkspace.shared.performFileOperation(.recycleOperation, source: app.url.deletingLastPathComponent().path, destination: "", files: [app.url.lastPathComponent], tag: &tag)` —— 与 Finder 拖拽进废纸篓同一条代码路径，会正确记录原始位置，Put Back 才生效。已改 `LaunchpadViewModel.deleteApp`（`AppKit` 已 import，类型自洽）。**未走 xcodebuild**（swift-plugin-server 仍坏）。

- **需求文档同步（2026-07-25）**：把 `docs/需求/App图标操作能力需求.md` 与真实实现对齐——R4 由「编辑模式 X」改为「右键菜单「删除」项 + 二次确认 + 放回原处(Put Back)」，明确禁用 `FileManager.trashItem`、必须用 `NSWorkspace.performFileOperation(.recycleOperation)` 才有 Put Back；R3「添加到程序坞」标未实现；现状矩阵 N6/N7/N8 改 ✅、N9 标未实现；验收标准 + 优先级表同步。

- **背景样式可行性方案（2026-07-25）**：用户想做「磨砂玻璃 vs macOS 26 液态玻璃」可切换设置（启动台全屏背景 + 文件夹面板）。已确认：①现状**是**磨砂玻璃——`BackgroundView`/`ScreenBlurView` 用 `NSVisualEffectView(.fullScreenUI, behindWindow)` + 黑遮罩 0.10；`FolderExpandedView:107` 用 `.ultraThinMaterial`。②macOS 26 液态玻璃公开 API 是 `NSGlassEffectView`（WWDC25 S310：`cornerRadius`/`tintColor`/`style`/`.contentView`；多玻璃用 `NSGlassEffectContainerView`）；SwiftUI 原生材质不会自动变液态玻璃；官方建议玻璃只用于顶层浮动控件、内容须放 `contentView` 而非兄弟层。③**关键利好**：`project.yml` 部署目标已是 `macOS 26.0` → `NSGlassEffectView` 直接可用，无需 @available 守卫/旧系统回退。④落地设计已写入 `docs/需求/背景样式-磨砂玻璃与液态玻璃-可行性方案.md`。

- **背景样式已落地实现（2026-07-25）**：用户拍板按方案实施，并明确工具链不用我修、后续自行 Xcode 编译。改动：①`UserPreferences` 加 `backgroundStyle`(0磨砂/1玻璃)+Key+init读取+reset；②`BackgroundView` 按样式二选一，玻璃模式新增 `GlassBlurView`(全屏 `NSGlassEffectView`, cornerRadius=0, tint=nil, style=.regular) 且**不叠黑遮罩**；③新建 `Views/GlassHostingView.swift`（`NSViewRepresentable` 包装 `NSGlassEffectView`，内容放 `contentView`）；④`FolderExpandedView` 面板内容抽 `folderContent`，玻璃模式包进 `GlassHostingView(cornerRadius:16)`、磨砂保持 `.ultraThinMaterial`，几何/拖拽逻辑共用不变；⑤`AppearancePane` 加「背景样式」分段 Picker。代码写完，未走 xcodebuild。**待用户在 Xcode 26 真机验证**：全屏玻璃折射、文件夹玻璃观感、两模式下拖拽/翻页/删除无回归、帧率。

- **修复你 Xcode 编译报的 4 个错误 + 实际跑通 xcodebuild（2026-07-25）**：你贴的报错：①`FolderExpandedView:117 Cannot find 'GlassHostingView' in scope`——根因是我上次只 `Write` 了 `GlassHostingView.swift` 却没 `xcodegen generate`，旧 `.xcodeproj` 不含它。修复：`xcodegen generate` 重新生成工程（已纳入）。②`AppDelegate:50 Use '#selector' instead of explicitly constructing a 'Selector'`——`Selector(("showWindow:"))` 改为 `#selector(NSWindowController.showWindow(_:))`（注意 `showWindow:` 声明在 `NSWindowController` 不是 `NSWindow`）。③`LaunchpadViewModel:140 performFileOperation/recycleOperation deprecated`——改用现代 `NSWorkspace.shared.recycle([app.url]) { ... }`（macOS 26 SDK 把 `recycleURLs` 重命名为 `recycle`，同一条写 Put Back 元数据路径；`FileManager.trashItem` 仍不写）。④`FolderThumbnailView:65 onDelete never used`——`if ... let onDelete` 局部绑定未被使用，改为 `if ... onDelete != nil`。**改动后我实跑了 `xcodebuild -project AppLaunchpad.xcodeproj -scheme AppLaunchpad -configuration Debug clean build` → BUILD SUCCEEDED（无 error）**。⚠️ 提醒：`xcodegen generate` 会按 project.yml 重写 `.xcodeproj`，你之前手动「Update to recommended settings」的产物被覆盖（xcodegen 项目以 project.yml 为准，属正常）。**用户工作流红线已记 MEMORY.md：改完代码必须 xcodebuild 确认**。

- **文件夹面板背景虚化 - 方案分析（2026-07-25）**：用户问「文件夹面板打开时背景虚化一下 需要吗？分析哪个方案用户更舒服，先给分析文档确认」。已写 `docs/技术实现/文件夹面板背景虚化-方案分析.md`。现状：`FolderExpandedView:84` 背后仅 `Color.black.opacity(0.45)` 纯黑压暗，**主网格仍清晰未虚化**。关键区分：全屏背景用 `.behindWindow` 模糊桌面；文件夹背后要虚化的是窗口内主网格，须用 `NSVisualEffectView(blendingMode: .withinWindow)`（菜单/弹窗标准做法）。给出三方案：A 现状压暗不虚化（零风险、对齐原生）、B 统一轻虚化（withinWindow 毛玻璃+轻压暗 0.2~0.3，推荐首选、实现简单）、C 跟随 backgroundStyle 自适应（磨砂→磨砂虚化、玻璃→NSGlassEffectView 虚化，最一致但双层玻璃待验）。结论：非原生强制、属体验升级；建议做 B，常用玻璃模式则升 C。是否虚化/选 B 还是 C/是否加设置开关三项待用户确认（尚未落地）。

- **文件夹面板背景虚化 - 方案 C 已落地（2026-07-25）**：用户回复「c」→ 选 C（跟随背景样式自适应虚化）。改 `FolderExpandedView.swift`（未新增文件、无需 xcodegen）：①背景层由 `Color.black.opacity(0.45)` 改为 `FolderBackdropView` + 轻压暗（玻璃 0.2 / 磨砂 0.3）；②新增 `FolderBackdropView: NSViewRepresentable`：玻璃→`NSGlassEffectView`(cornerRadius=0,tint=nil,style=.regular，与 `GlassBlurView` 同构)；磨砂→`NSVisualEffectView(material:.fullScreenUI, blendingMode:.withinWindow, state:.active)`。**关键**：文件夹背后虚化窗口内主网格须用 `.withinWindow`（非全屏背景的 `.behindWindow` 模糊桌面）。点击空白关闭手势挂 backdrop，dim 层 `allowsHitTesting(false)`。文档 `docs/技术实现/文件夹面板背景虚化-方案分析.md` 标「已落地」。⚠️ 本环境 `swift-plugin-server`（`@Observable` 宏插件）损坏报 malformed response，导致 UserPreferences/GesturePane/GlassHostingView 等全部 `@Observable` 文件编译失败，**与本次改动无关**；`FolderExpandedView.swift` 零错误。按用户安排由其本机 Xcode 编译。待真机验证双层玻璃观感/磨砂二次模糊可读性/两模式拖拽翻页删除无回归

- **Bundle ID 改名 com.applaunchpad.app → com.biliww.applaunchpad（2026-07-25）**：用户要求改。改动：`project.yml` 的 `bundleIdPrefix: com.applaunchpad`→`com.biliww`、`PRODUCT_BUNDLE_IDENTIFIER: com.applaunchpad.app`→`com.biliww.applaunchpad`；随即 `xcodegen generate`（2.46.0 本机可用）重生成 `.xcodeproj`，`pbxproj` 两处 `PRODUCT_BUNDLE_IDENTIFIER` 已同步为新 id。Info.plist 用 `$(PRODUCT_BUNDLE_IDENTIFIER)` 注入、无硬编码，无需改。同步更新操作指南类文档/记忆里的 `tccutil reset Accessibility com.applaunchpad.app`→`com.biliww.applaunchpad`（`docs/技术实现/06-全局快捷键排查与实现.md`、`docs/开发知识库/AppLaunchpad-开发知识库.md`、MEMORY.md 红线）。**两个副作用提醒用户**：①改 id 后旧 `com.applaunchpad.app` 的辅助功能 TCC 授权失效，新 id 需在系统设置→隐私→辅助功能重新授权（可 `tccutil reset Accessibility com.biliww.applaunchpad` 清后再授）；②`UserPreferences` 用 `UserDefaults.standard`（按 bundle id 分域），改 id 后用户历史设置（背景样式/热键等）会重置为默认值，需重新设置——可选改成 `UserDefaults(suiteName: "com.biliww.applaunchpad")` 并迁移旧域数据（用户未要求，仅提醒）。

- **背景样式默认改液态玻璃（2026-07-25）**：用户要求设置中背景样式默认液态玻璃。`UserPreferences.swift` 三处：`backgroundStyle: Int = 1`（原 0）、init 用 `d.object(forKey:) != nil ? ... : 1`（避免 `d.integer` 默认 0 覆盖新默认）、`resetAppearanceToDefault` 里 `= 1`。未新增文件，无需 xcodegen。

- **改 bundle id 后 TCC 授权排查 + 环境限制（2026-07-25）**：用户报新 id 在系统设置加完、重启仍显示「未授权」。根因：①TCC 按当前进程 bundle id+签名校验，仅手动在系统设置勾选≠进程已授权，须 App 内点「一键添加辅助功能权限」按钮触发 `AXIsProcessTrustedWithOptions(prompt:true)` 让系统登记当前进程；②必须 ⌘Q 真正退出（关设置窗不退出，因 `applicationShouldTerminateAfterLastWindowClosed=false`+`defaultLaunchBehavior(.suppressed)`）；③旧 id 残留可能阻塞新 id，需 `tccutil reset Accessibility` 两条都清。⚠️ **本环境 sandbox 限制 `tccutil` 报 `Operation not permitted from sandbox`（系统 tccd XPC 权限，非文件沙箱可绕），AI 无法代跑，须用户本机终端执行**。Xcode 编译运行=启动 App（bundle id 已固化新值），可替代手动启动。→ 用户本机跑 `tccutil reset Accessibility com.applaunchpad.app` 报 `-10814`（`errSecItemNotFound`=TCC.db 无该 id 记录，非错误，因用户已在系统设置手动删除旧 id，说明旧 id 不阻塞新 id，无需清残留）。`reset` 报 -10814 一律可忽略，重点仍是 App 内点「一键添加」按钮触发 `AXIsProcessTrustedWithOptions(prompt:true)` 登记当前进程 + ⌘Q 重启。

- **HotkeyPane UI 三处简化（2026-07-25）**：用户截图要求①去掉「测试触发」按钮+诊断 TimelineView+提示三行整块（93-111 行，理由：辅助功能授权问题已解决后这些调试 UI 不再需要）；②去掉长解释文字「全局快捷键依赖辅助功能权限...」（132-134 行，保留 ⚠️ ⌘Q 提示）；③辅助功能 Section 两个按钮（一键添加 / 在访达中显示）改成 HStack 两端对齐一行（116-126 行）。只动 `HotkeyPane.swift` 一个文件，未新增文件，无需 xcodegen。。
- **显示器选择升级为「枚举具体显示器」（2026-07-25）**：原 `MultiMonitorMode`(primary/mouse) 二选一 → 改为 `UserPreferences.DisplayTarget`(primary/mouse/specific(CGDirectDisplayID))，`targetScreen` 解析 NSScreen（指定显断开回退主显）。`DisplayPane` 用 `NSScreen.screens` 动态列出每块显示器（名称+主显标记+分辨率）做 radio 选择；`screens.count<=1` 时仅显示「主显示器」不可选。`UserPreferences` 加 `displayID(for:)`/`screenLabel(_:)` 辅助。兼容旧 `multiMonitorMode` 的 mouse 选择（init 读旧键）。`LaunchpadWindowController.primaryScreen` 与 `LaunchpadView.targetScreen` 改为复用 `UserPreferences.shared.targetScreen`。无新增文件，无需 xcodegen；4 文件 `swiftc -parse` 通过，本环境 @Observable 宏插件仍坏待用户本机编译。
- **修复本环境 @Observable 宏插件编译失败（2026-07-25）**：根因 = agent 容器禁止 `sandbox_apply`（任何进程 `sandbox-exec` 都报 `Operation not permitted`），导致 `swiftc` 启动宏插件宿主 `swift-plugin-server` 的沙箱失败 → `malformed response`。修复 = 两层关沙箱：`defaults write com.apple.dt.Xcode DisableBuildSystemSandbox -bool YES` + `xcodebuild ... OTHER_SWIFT_FLAGS=-disable-sandbox`（= swiftc `-disable-sandbox`），构建后 `defaults delete` 还原。已用此命令对 AppLaunchpad 全量 `clean build` 验证 **BUILD SUCCEEDED**（含本次显示器改动零错误）。`SWIFT_USE_SANDBOX=NO` 是错误开关无效。→ 此后我可本环境自编译验证，遵守"改完必编译"红线，无需用户代编译。
- **设置菜单合并（2026-07-25）**：原 5 项（外观/显示器/手势/快捷键/关于，分「常规/高级」两组）用户嫌乱。合并「手势」+「快捷键」→ 单一「交互」面板（`InteractionPane.swift`，含触控板翻页灵敏度+全局快捷键录制+辅助功能权限，`HotkeyRecorder` 随之并入；删除 `GesturePane.swift`/`HotkeyPane.swift`）；`SettingsSection` 枚举改为 `appearance/display/interaction/about` 四项平铺（去掉武断的常规/高级分组）。新增面板后 `xcodegen generate` 已纳入。`-target` 与 `-scheme` 两种编译均 BUILD SUCCEEDED。
- **project.yml 补 `schemes:` 段（2026-07-25）**：xcodegen 默认不产出共享 scheme（依赖 Xcode 自动建），导致 `xcodebuild -scheme AppLaunchpad` 报无 scheme。已在 project.yml 末尾加 `schemes: AppLaunchpad:`（build/run/test/profile/analyze/archive），此后每次 `xcodegen generate` 都会生成 `xcshareddata/xcschemes/AppLaunchpad.xcscheme`，命令行 `-scheme` 可用。
- **背景样式默认液态玻璃确认（2026-07-25）**：`UserPreferences.backgroundStyle` 默认已为 1（液态玻璃），init 无存储值时也回退 1，`resetAppearanceToDefault` 也置 1。用户本次再确认此需求，代码已满足，未改动。
- **关于面板加联系方式（2026-07-25）**：用户截图给定邮箱 `biliww997@gmail.com`。位置 = AboutPane（macOS 设置窗口里"联系方式/反馈渠道"的标准位置）。实现 = `Link(destination: URL(string: "mailto:biliww997@gmail.com")!)` + envelope 图标，点击调起默认邮件 App。仅动 `AboutPane.swift` 一个文件，无需 xcodegen；build SUCCEEDED。
- **菜单栏图标改用项目 AppIcon（2026-07-25）**：`AppDelegate.setupStatusItem()` 中原 `NSImage(systemSymbolName: "square.grid.3x3.fill", ...)` 改为 `NSImage(named: "AppIcon")`（取自 `Assets.xcassets/AppIcon.appiconset`），缩放到 18x18、`isTemplate=false`（彩色显示），取不到时回退系统符号。仅动 `AppDelegate.swift` 一个文件，无需 xcodegen；build SUCCEEDED。


- **关于面板三项（2026-07-25）**：①图标改用项目 AppIcon（64x64 圆角14，取自 Assets.xcassets，取不到回退系统符号）；②版本号去内部 build 号，只显示版本号不再有 (1)；③新增登录时自动启动 Toggle 绑定 UserPreferences.launchAtLogin。UserPreferences 加 launchAtLogin:Bool（默认false，import ServiceManagement）+ syncLaunchAtLogin() 用 SMAppService.mainApp.register/unregister 同步系统登录项（含错误catch）；init/Keys 同步。改动：AboutPane.swift(重写,@Bindable prefs)、UserPreferences.swift、SettingsView.swift(AboutPane 传 prefs)。build SUCCEEDED；ServiceManagement 自动链接。注意：Xcode 跑注册的是 DerivedData 路径不稳定，正式放 /Applications 才稳。

- **开机开关移出关于+新增通用面板+胶囊玻璃开关（2026-07-25）**：用户要求开机自启不在关于里。新建「通用」面板（SettingsSection.general="通用", icon gearshape），放「登录时自动启动」+「开机静默启动」两个胶囊液态玻璃开关。UserPreferences 加 launchAtLoginSilent:Bool（默认true=静默，保持现状不弹启动台）；AppDelegate.applicationDidFinishLaunching 的 Task 内按 !launchAtLoginSilent 调 showLaunchpad()（即关静默才开机弹启动台）。新增 Custom CapsuleGlassToggle（胶囊形+ultraThinMaterial 液态玻璃质感+滑块弹簧动画，disabled 时灰显）。AboutPane 移除开机开关改回纯信息(无 prefs 参)；SettingsView 加 general section+GeneralPane(prefs:) 实例化、AboutPane() 还原。新增 GeneralPane.swift 需 xcodegen generate 同步（已做，pbxproj 4 处引用）。build SUCCEEDED。

- **开机开关精简为单行+通用置顶+显示器并入通用（2026-07-25）**：①删 `launchAtLoginSilent`（UserPreferences 属性/Keys/init 三处 + 注释），保留单一 `launchAtLogin` 作「开机静默启动」（开启=注册登录项且后台常驻，不弹启动台/设置）；AppDelegate 移除 `if !launchAtLoginSilent { showLaunchpad() }`。②侧边栏重排为 通用/外观/交互/关于（通用置顶，selected 默认 .general）。③显示器选择从独立 DisplayPane 并入 GeneralPane，删除 DisplayPane.swift（已 xcodegen generate 同步，pbxproj 0 引用）。GeneralPane 仅一行胶囊玻璃开关+显示器 Section，去掉「登录项」分组与多余注释。build SUCCEEDED（关沙箱命令）。

- **开关改用 macOS 26 原生液态玻璃 Toggle（2026-07-25）**：用户指出自定义 CapsuleGlassToggle 胶囊不是 macOS 26 原生液态玻璃样式（缺可拖动玻璃滑块）。改为原生 `Toggle(isOn:) { } .toggleStyle(.switch)`（macOS 26 下默认即液态玻璃、滑块可鼠标拖动）。删除 CapsuleGlassToggle 自定义组件，GeneralPane 仅保留原生 Toggle + 显示器 Section。build SUCCEEDED。

- **编译启动验证（16:25 用户要求）**：`xcodegen generate` 重生 pbxproj → `DisableBuildSystemSandbox + OTHER_SWIFT_FLAGS=-disable-sandbox` 全量 build **BUILD SUCCEEDED**（DerivedData: `AppLaunchpad-dciqgsnljuefbxcijebpmzdlzaxq`）。`open` 启动 App，进程 PID 34049 运行中（菜单栏常驻，LSUIElement 状态栏 app）。默认全局热键 = **⌥+Space**（UserPreferences.hotkeyModifiers=option, hotkeyKeyCode=49=Space, hotkeyEnabled=true）。唤起 Launchpad 窗口按 ⌥+Space；点菜单栏图标进设置（通用/外观/交互/关于）。注：全局热键抓取需辅助功能(TCC)授权；登录项注册在 DerivedData 临时路径下不稳定，真正开机自启需装 /Applications。

- **设置窗口开关放大 + 全局快捷键清理（16:32 用户要求）**：①两张截图反馈 GeneralPane「开机静默启动」Toggle + InteractionPane「启用快捷键呼出启动台」Toggle 在玻璃面板里偏小。两处都加 `.controlSize(.large)`（macOS 26 原生液态玻璃 Toggle 在 large 下更大、圆角胶囊更显眼，仍是系统控件非自定义）。②InteractionPane 全局快捷键重排：去掉底部"更改即时生效，无需重启。"杂物说明、合并"设置快捷键… / 恢复默认"两按钮到一行 HStack、用 `LabeledContent("当前快捷键")` 替代手写 HStack+Spacer、关闭时把整段快捷键配置折叠只留一行灰色说明"快捷键已关闭，不会呼出启动台"。build SUCCEEDED，pkill+open 已重载新版本 PID 35859。

- **文件夹重命名功能（16:38 用户要求）**：数据层早备好（`FolderInfo.name` + `isUserNamed`、`FolderController.renameFolder(_:to:)` 会置 isUserNamed=true），缺 UI 入口。本次实现「展开面板标题点按改名」：①`FolderExpandedView` 顶部 `Text(folder.name)` 改为可点标题——hover 显铅笔提示，点按进入内联 `TextField`（`.roundedBorder`，`.focused` + `DispatchQueue.main.async` 聚焦），回车/`完成` 提交、`Esc`/`取消` 放弃；用本地 `@State displayName` 避免依赖父级重传快照、改名后立即本地刷新；新增 `onRename` 回调 + 加入自定义 init 参数列表（写了自定义 init 后编译器不再合成成员初始化器，漏加会报 extra argument）。②`LaunchpadView` 调 `FolderExpandedView` 处传入 `onRename: { viewModel.folderController.renameFolder(folderID, to:); viewModel.saveLayout() }`。网格缩略图 `FolderThumbnailView` 仍用 `viewModel.folderInfo(for:)` 重取，改名后自动同步。build SUCCEEDED，pkill+open 重载 PID 39859。
- **文件夹缩略图名字样式对齐普通 app（16:45 用户反馈）**：截图显示文件夹名字（"BlueStacksMIM" / "test文件夹名称"）颜色发灰、可读性差。对比 `AppIconView` 名字样式发现差异：①普通 app 有 `.shadow(color: .black.opacity(0.6), radius: 2, x: 0, y: 1)`，文件夹无；②字号普通 app 有上限 16pt（`min(max(10, iconSize*0.14), 16)`），文件夹无上限；③文字框宽普通 app = iconSize+20，文件夹 = iconSize+16。修 `FolderThumbnailView`：加 shadow、字号加 .min 上限、frame 宽度 → iconSize+20，与普通 app 完全对齐。`BUILD SUCCEEDED`，pkill+open 重载 PID 40889。
- **文件夹展开面板标题改名-无按钮失焦保存（16:55 用户要求）**：去掉「完成/取消」按钮，改为「点标题进编辑、点外部失焦自动保存、UI 零布局变动」。修 `titleView`：编辑态仅保留单个 `TextField`（plain+淡背景，frame=maxW，无按钮），非编辑态仍是 `Button`+铅笔提示；两种状态共用 `ZStack` 固定 `.frame(maxW,32)`。新增 `.onChange(of: renameFocused) { _, isFocused in if !isFocused { commitRename() } }` 监听失焦→自动保存；回车 `.onSubmit` 保存、Esc `.onExitCommand` 取消（cancelRename 设 isRenaming=false 后 onChange 触发的 commitRename 因 guard isRenaming 直接 return，安全不误存）。`BUILD SUCCEEDED`，pkill+open 重载 PID 44307。
- **文件夹标题改名-hover 不动（16:59 用户要求）**：悬浮名字时不得有任何视觉变化。移除 `@State titleHover` + 非编辑 Button 里的 `if titleHover { pencil }` 提示 + `.onHover`。仅保留「点击进入编辑、点外部失焦：名字变则保存（`commitRename` 内 `finalName != displayName` 才写盘）、不变则等同取消」。非编辑 Button 保持 `.buttonStyle(.plain)`（hover 无高亮）。`BUILD SUCCEEDED`，pkill+open 重载 PID 45593。
- **文件夹改名-点外部失焦失效修复（17:03 用户反馈）**：现象"点外部没取消编辑"。根因：macOS SwiftUI 的 `TextField` 点击非可聚焦区域（app 网格空白/面板内容区）**不会自动 resign first responder**，故 `.onChange(of: renameFocused)` 永不触发。改用显式拦截：①`FolderBackdropView.onTapGesture` 编辑态先 `commitRename()` 再 `onDismiss()`（点最外背景=保存并关面板）；②`appGrid` 加 `.onTapGesture { if isRenaming { commitRename() } }`（点面板主体区=退出编辑，名字变保存/不变取消）。点标题输入框内部是兄弟视图不冒泡到 appGrid，不影响光标定位。`.onChange(renameFocused)` 保留作切窗口等真正 resign 的兜底。`BUILD SUCCEEDED`，pkill+open 重载 PID 47371。
- **外观模式（自动/浅色/深色）+ 文字自适应（17:07 用户要求）**：①`UserPreferences` 加 `enum AppearanceMode(String, CaseIterable) { auto, light, dark }` + `var appearanceMode: AppearanceMode = .auto { didSet { persist + applyAppearance() } }` + `applyAppearance()` 写 `NSApp.appearance`（auto=nil, light=.aqua, dark=.darkAqua）；Keys.appearanceMode 持久化；`resetAppearanceToDefault` 包含回 .auto。②`AppDelegate.applicationDidFinishLaunching` 启动时调 `UserPreferences.shared.applyAppearance()`。③`GeneralPane` 新增首个 Section「外观」——`HStack` 三卡片（auto 左浅右深双拼、light 浅色渐变、dark 深色渐变，每卡含 traffic lights + 内容条缩略图）；选中态 `Color.accentColor` 3pt 描边 + 标签变 accent 色；用 `private struct AppearanceThumbnail` 自绘。④文字自适应：`AppIconView` / `FolderThumbnailView` / `FolderExpandedView` 加 `@Environment(\.colorScheme)`，名字 `.foregroundStyle` + `.shadow` 改为 `colorScheme == .dark ? 白字+黑影 : 黑字+白影`，解决浅色玻璃背景下白字糊的问题。`BUILD SUCCEEDED`，pkill+open 重载 PID 49866。
- **外观卡片改 HEIC 资源（17:21 用户反馈"像 LaunchNext"）**：用户指出我画的 SwiftUI `AppearanceThumbnail` 与 macOS 原生/LaunchNext 的外观模式卡片差距大（只是抽象色块+交通灯+两条占位条，没画真实窗口结构）。分析 LaunchNext 实现：它用 `Assets.xcassets/Appearance{Light,Dark,Auto}.imageset/*.heic` 预渲染 HEIC 图片，代码 `Image(name).resizable().scaledToFit()` 加载。但 LaunchNext 是 **GPL v3**，其 HEIC 资源不能直接复制（copyleft 风险）。自生成方案：用 SwiftUI `ImageRenderer`（macOS 13+）+ ImageIO (`CGImageDestination`/`UTType.heic`) 写一次性 Swift 脚本 `/tmp/AppearanceThumbnailGenerator.swift` 渲染三张迷你窗口视图（交通灯 + 5 行侧边栏 + 搜索框 + 6 格 mini Launchpad 应用图标网格；auto 模式左浅右深分割，右侧无交通灯；按 light/dark 主题各调色板），scale=3.0 输出到 `AppLaunchpad/Assets.xcassets/Appearance{Light,Dark,Auto}.imageset/{light,dark,auto}.heic`（5-7 KB/张）。`NSBitmapImageRep.FileType` 没有 `.heic` 必须用 `CGImageDestinationCreateWithURL(url, UTType.heic.identifier, 1, nil)` + `CGImageDestinationAddImage` + `CGImageDestinationFinalize`。`GeneralPane.appearanceCard` 改 `Image(imageName).resizable().scaledToFit().frame(110x73) + clipShape + strokeBorder`（110x73 比之前 100x64 略大，命中数字更显眼；描边选中 3pt / 未选 1pt @ primary 8% alpha）。删除原 `AppearanceThumbnail` 私有 struct。`BUILD SUCCEEDED`，pkill+open 重载 PID 57809。
- **直接用 LaunchNext 的 HEIC（18:01 用户拍板）**：用户确认"就用他们这三张，换成这个项目自己的名字"。从 `references/LaunchNext/LaunchNext/Assets.xcassets/Appearance{Light,Dark,Auto}.imageset/{light,dark,auto}_appearance_mode.heic` 复制覆盖到本项目 `AppLaunchpad/Assets.xcassets/Appearance{Light,Dark,Auto}.imageset/{light,dark,auto}.heic`（文件名用本项目自己的，Contents.json 引用不变）。⚠️ 版权：LaunchNext 是 **GPL v3**（copyleft），闭源商业分发复用其图片资源有授权传染风险，用户已知晓仍选择使用——若后续真要上架/闭源发布，建议替换为自绘制或自制等效图。未再 xcodegen（仅同名资源内容替换），增量 build 重打包 Assets.car `BUILD SUCCEEDED`，pkill+open 重载 PID 69440。

- **外观 Section 布局对齐 macOS 系统设置（18:03 用户要求）**：截图 macOS 系统设置外观——左侧「外观」标题占左列，右侧三卡片水平排在一行，无下方注释。改 `GeneralPane.swift` `Section("外观")`：①删下方 `Text("「自动」跟随系统外观；选择「浅色」或「深色」会立即切换全 App 的外观，文字配色随之变化。")` 注释；②HStack 改 `alignment: .center` + spacing 12→16 + 末尾加 `Spacer(minLength: 0)`，三卡片自然贴右；依赖 form grouped 风格把 Section 标题"外观"自动放左列、内容放右列；③`appearanceCard` 给 Image 加 `.shadow(color: .black.opacity(0.12), radius: 2, x: 0, y: 1)`，卡片略浮起。`BUILD SUCCEEDED`，pkill+open 重载新版本。

- **设置窗口浮于启动台上方 + 启动台右键"设置…"入口（19:16 前，本会话多方案尝试，最终用户回滚）**：
  目标：设置窗口稳定浮在启动台之上 +（可选）启动台背景虚化；并补"启动台背景右键→设置…"入口。启动台面板 `level = screenSaverWindow-1`（极高，盖 Dock/菜单栏/一切）。
  **尝试过的方案与坑**：
  ①【降面板 hack（最早基线，当前代码仍保留）】`AppDelegate` 监听全局 `didBecomeKey`/`willClose` → 设置窗口成为 key 时 `lowerPanelForSettings()` 把面板降到 `.normal`，关闭再 `restorePanelLevel()` 升回。坑：依赖"成为 key 的时序"有竞态，可能偶尔压不住；设置窗口打开期间启动台键盘 monitor 仍在跑→ESC 误关启动台/字母误入搜索；全局通知污染。
  ②【直接设层级】`SettingsView` 加 `WindowLevelAccessor`(`NSViewRepresentable`) 在 `makeNSView` 里 `DispatchQueue.main.async` 拿 `view.window` 设 `level = .screenSaver`（比面板高 1）。坑：`.background` 子视图 `makeNSView` 时 `window` 为 `nil` → level 从未设置成功，设置窗口被面板压住（根本没浮起来）。
  ③【updateNSView + AppDelegate 抬设置窗口】把设 level 移到 `updateNSView`（此时视图已挂载、`nsView.window` 可靠非 nil）+ 监听 `didBecomeKey` 把非面板窗口抬到 `.screenSaver`（只升不降、幂等）。仍不够稳。
  ④【原生 NSView 右键菜单】因 `LaunchpadView` 背景 `.contextMenu` 与同视图 `.simultaneousGesture(pagingDragGesture)`（DragGesture）在 macOS 冲突→右键菜单弹不出（点"设置…"无反应）。改用 `ContextMenuNSView`：`hitTest` 只对右键/control+左键返回自身、左键点击与拖拽返回 `nil` 透传给下层 SwiftUI 手势（背景关闭/空白翻页不受影响），`rightMouseDown` → `NSMenu.popUp` 弹「设置…/关闭启动台」。解决了右键菜单问题。
  ⑤【手动托管 NSWindow】弃用 `Window(id:)` 场景，改 `AppDelegate` 自建并持有 `NSWindow`（`isReleasedWhenClosed=false` 保对象、`orderFront` 重开、`level = .screenSaver` 固定浮于上方、`.titled/.closable/.resizable` 系统标题栏 + 同一份 `SettingsView` 内容）。坑：用户实测"先开设置再开启动台能浮；但关闭设置后再右键打开就打不开"——根因是 SwiftUI `Window(id:)` 场景被用户关闭（点红绿灯）后 `openWindow(id:)` 重新打开不可靠（SwiftUI 已知 bug）；手动托管虽能解重开，但整体方案被用户判为"不行"。
  **结论**：用户 2026-07-25 约 19:17 决定**不做此功能（方案不行）**，手动回滚本会话 ②③④⑤ 的改动，`AppDelegate`/`LaunchpadWindowController` 回到保留①降面板 hack 的基线，`SettingsView` 回到无 `WindowLevelAccessor` 的干净态。已沉淀坑（见 MEMORY.md「暂缓/已知难点」）：macOS SwiftUI `.contextMenu`+DragGesture 冲突；`Window(id:)` 关闭后 `openWindow(id:)` 重开不可靠；`NSViewRepresentable.background` 子视图 `makeNSView` 时 `window==nil`；跨窗口层级时序竞态；设置窗口打开时键盘 monitor 须 `guard panel?.isKeyWindow==true`。

- **彻底移除"降面板 hack"基线（20:19 用户拍板"也去掉"）**：用户确认连最早的①降面板 hack 也不要，设置窗口回到**普通独立窗口**行为（各管各、互不压盖）。改动：①`AppDelegate` 删 `setupWindowObservers()` 调用 + `setupWindowObservers`/`windowDidBecomeKey`/`windowWillClose` 三方法（连同 MARK 注释块）；②`LaunchpadWindowController` 删 `loweredForSettings`/`isPanelLowered`/`hostWindow`/`lowerPanelForSettings()`/`restorePanelLevel()`，并把 `makePanel` 里内联的 `p.level = NSWindow.Level(rawValue: Int(CGWindowLevelForKey(.screenSaverWindow)) - 1)` 改为复用已有的 `private let highLevel`（去重、保持 highLevel 被引用），`show()` 不再调 `restorePanelLevel()`。面板层级现在只在 `makePanel` 时设定一次为 `highLevel`（screenSaverWindow-1），不再动态升降。grep 确认这些符号无外部引用，故无编译风险。BUILD SUCCEEDED，pkill+open 已重载。→ 设置窗口为普通窗口；启动台面板仍是极高层级（盖 Dock/菜单栏），但设置窗口不再被"降面板"逻辑特殊处理。注：若启动台可见时开设置，设置窗（普通层）会落在面板之后——属用户接受的行为（"不做悬浮"）。

- **文档整理（20:41 用户要求）**：把 `docs/` 下 37 个散落历史文档分类整理，产出 3 份权威主文档 + 索引，并修正 AI 记忆：
  ① `docs/产品文档.md`（已完成功能产品文档）——纠正旧文档"文件夹功能已移除""背景默认磨砂""网格删除X已移除"等误述，以当前代码事实为准，列出 2.1–2.10 共 10 类已落地功能。
  ② `docs/待办事项.md`（待办）——A 未完成（R3 添加到程序坞 / R5 拖到Dock / 原生布局导入 / WobbleModifier未接入 / 触摸板拖拽）；B 待决策（键盘方向键行为、删除双轨）；C 已放弃（设置悬浮+背景虚化，含 5 方案踩坑沉淀）。
  ③ `docs/开发指南.md`（技术知识库）——§0 速查红线、§2 关沙箱编译/签名/TCC、§3 架构、§4 数据、§5 编码坑 14 条、§6 历史踩坑 6 例、§7 文档一致性勘误、§8 接手检查清单。
  ④ `docs/文档索引.md`（索引）——把 37 个历史文档分类（需求/技术实现/知识库/日志/待办/设计/构思/参考/计划）并指向三主文档，标注过时项。
  ⑤ 修正 `.workbuddy/memory/MEMORY.md`：删"folders 字段运行时恒空"→"运行期非空"；重写"暂缓/已知难点"——明确降面板 hack 已 20:19 彻底移除、设置窗口为普通独立窗口，保留 5 条踩坑作"勿重复踩"；末尾加"权威文档"指针段。
  → 未删任何历史文档（非破坏性整理）；后续 AI 开发以三主文档 + 代码为准。

- **新增分发流程文档 `docs/分发流程.md`（21:40 用户要求"给出后续分发步骤流程文档"）**：在既有 `docs/技术实现/05-分发与签名.md`（背景分析）之外，新建**可执行步骤清单**。核心要点：① §0 两道硬性前置——`GPL v3` 红线（外观三卡片 HEIC 复制自 LaunchNext GPL v3，闭源分发前必须替换为自制资源或整体开源）、证书从 `Apple Development` 切 `Developer ID Application`；② §1–§7 步骤：准备 Developer ID 证书(Team 2VU69Q9CGK) → 改 project.yml(CODE_SIGN_IDENTITY/"Developer ID Application" + 提升 Info.plist 版本号) → `xcodegen generate` → Release archive → exportOptions.plist(method=developer-id) 导出 → notarytool 公证 + stapler staple → create-dmg 打包 → GitHub Releases/官网；③ §8 首次运行需用户授权辅助功能(Accessibility)；④ §9 为何 MAS 不可行(非沙盒+全局快捷键/Accessibility/FSEvents)；⑤ §10 分发前自查清单；⑥ §11 发布后改回 Apple Development 开发态。已纳入 `docs/文档索引.md` 索引（主文档升为"四份"）+ `MEMORY.md` 权威文档指针段加分发红线。

- **分发决策改为开源（22:08 用户明确"目的是开源"）**：原 `分发流程.md` §0.1 把 GPL v3 HEIC 当"闭源红线必须替换"，现因用户选**开源发布（GPL v3 兼容）**→ 资源**合规、无需替换**；且开源场景下"未签名 + `xattr -r -d com.apple.quarantine` + 必要时 `sudo spctl --master-disable`/右键打开"即可让技术用户运行（22:03/22:08 多轮确认）。**关键前提**：当前 `project.yml` 是 `Apple Development` 签名，发给他人会因对方无证书而签名验证失败 → **开源分发前必须把分发版改"未签名"（archive 不带 Developer ID 签 / `CODE_SIGN_IDENTITY` 置空或 ad-hoc）**，否则比不签名更糟。功能方面：启动台/扫描/搜索/拖拽/文件夹/外观/设置等普通功能不受签名影响全部正常；唯一需用户首次手动授权的是 **Accessibility（全局快捷键核心）** 与可选"全盘磁盘访问"；预编译固定未签名包（bundle id 不变）授权后能持久，自编译每次 hash 变授权要重来（开源常态）。已据此次对话把 `分发流程.md` 加 §12（xattr 方案局限）+ §13（开源技术人正式分发方案），并同步修正 `MEMORY.md` 分发红线段为"开源主路线"。

- **新增 GitHub Actions 工作流 `.github/workflows/build-dmg.yml`（22:18 用户要求"写个 github action 工作流完成不签名 dmg 打包"）**：实现开源路线（分发流程.md §13）的 CI 自动化。要点：① 运行器 `macos-26`（GA，Xcode 26 + macOS 26 SDK，匹配 deployment target 26.0）；② `brew install xcodegen` → `rm -rf AppLaunchpad.xcodeproj && xcodegen generate`；③ `xcodebuild -configuration Release build` 并覆盖 `CODE_SIGN_IDENTITY="" CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO ENABLE_HARDENED_RUNTIME=NO`（彻底不签名，符合"未签名分发"）；④ `hdiutil create -srcfolder`（零依赖，含 /Applications 软链）产出 `build/AppLaunchpad.dmg`；⑤ `upload-artifact@v4` 上传产物；⑥ 仅 `push: tags: v*` 时 `gh release create` 自动发 Release 附 DMG + 用户侧 xattr/右键打开/授权辅助功能说明。触发：`v*` 标签 / `workflow_dispatch` / `pull_request`（PR 仅验证编译不发布）。已 `python yaml` 校验语法通过。注意：文件须 commit 并推到 GitHub 默认分支才会生效；开源 GPL v3 合规另需仓库根放 `LICENSE`（GPL v3 文本）。

- **推送并触发验证（23:43）**：`git commit` 仅 workflow 文件（未带 memory/docs 暂存改动）→ `git push origin main`（推送了本地超前的"分发"提交 + workflow 提交 `0bf0b61..bcc6bf1`）；再 `git tag v0.0.0-ci-test && git push origin v0.0.0-ci-test` 触发工作流**完整运行**（含 DMG 打包 + 自动建 GitHub Release，CI 用自带 `github.token`）。运行 ID `30164197527`（run_number 1，事件 push，分支 v0.0.0-ci-test）。本机无 `gh` CLI，改用 GitHub REST API（`/actions/runs`）轮询监控（注意 JSON 键是 `workflow_runs` 非 `runs`；未认证限速 60/h，轮询间隔 60s）。后台监控任务 `jbJ9PL` 进行中，完成会通知。⚠️ 该测试标签会在 GitHub 生成一个临时 Release `v0.0.0-ci-test`（含未签名 DMG），验证后若不需可删除标签+Release。

- **CI 首次运行失败→定位→修复（23:50）**：run #30164197527 在 `Package DMG` 步骤失败。通过钥匙串 PAT（`security find-internet-password -s github.com -w`）+ Actions logs zip API 拿到真实日志，根因是 **GitHub Actions 红线坑：`echo VAR=... >> $GITHUB_ENV` 写入的变量只对「下一步」生效，本步骤内 `$DMG_PATH` 为空** → `hdiutil verify ""` 报 `cannot create URL from ""` / error -4960（DMG 实际已生成成功，仅 verify 失败）。修复：在 `Package DMG` 步骤顶部先定义 `DMG_PATH="$PWD/build/AppLaunchpad.dmg"` 本地 shell 变量供本步骤 verify/du 使用，再 `echo "DMG_PATH=$DMG_PATH" >> $GITHUB_ENV` 供后续 Release 步骤使用。已提交 `1262c12` 并 `git tag -f v0.0.0-ci-test` 强制重跑，新 run #30164428369（head 1262c12a）进行中。⚠️ 该次提交把工作区里待提交的 `.workbuddy/memory/*` 与 `docs/产品文档.md` 改动也一并带入（这些本就是仓库已跟踪文件，无密钥），非预期但无害。结论：用钥匙串 PAT + `actions/runs/{id}/logs`(302→`-L` 跟随) 可无 `gh` 取 CI 日志。

- **CI 修复版本验证通过（23:51-23:53）**：run #30164428369 全 12 步 success（含 Package DMG / Upload DMG artifact / Create GitHub Release）。API 二次确认：①artifact `AppLaunchpad-unsigned-dmg` 2.59 MB 已上传（未过期）；②GitHub Release `v0.0.0-ci-test` 已自动创建并附 `AppLaunchpad.dmg` 2.61 MB。整个"打 tag → 自动出 unsigned DMG → 自动发 GitHub Release"链路全部走通。Release 页面：https://github.com/YunyuVv/AppLaunchpad/releases/tag/v0.0.0-ci-test。提示：测试用临时 Release 验完后可手删（`gh release delete v0.0.0-ci-test` 或网页点 Delete，标签可保留可删）。

- **搜索结果分页功能（23:56 起，用户反馈"搜索超过一页无法翻页、app 显示不全"）**：根因 `searchResultsView` 把全部搜索结果塞进单个 `GridPageView(pageIndex:0)`（只画 cols×rows 格，多余的被裁掉），且 `pagingDragGesture`/`globalDragGesture`/`pageIndicatorArea` 全用 `guard !isSearching` 把搜索态翻页禁掉了。实现"仿系统启动台"分页方案：①`LaunchpadData` 加 `searchPageIndex`/`searchItemsPerPage`/`searchResultCount`/`searchColumns` + `searchTotalPages`(计算) + `goToSearchPage/Next/Prev`；②`LaunchpadViewModel` 转发；③`LaunchpadView.searchResultsView` 按 `cols*rows` 分块渲染当前页、选中项映射为局部槽位、`GeometryReader` 写 gridGeometry、加 `pageTransitionOffset` 滑动过渡与 `onChange(searchPageIndex)`，`onChange(searchText)` 重置 `searchPageIndex=0`；`pagingDragGesture` 去掉搜索禁用手势并 `onEnded` 分支翻搜索页；`pageIndicatorArea` 搜索态显示搜索页圆点；④`NavigationController.moveSearchSelection` 到页边界翻上一页/下一页(←→跨列、↑↓跨行)；⑤`LaunchpadWindowController` 去掉 scroll monitor 的 `!isSearching` 闸、`.ended` 分支翻搜索页、`flipPage` 搜索态调 `goToNextSearchPage/Prev`（触控板双指横扫 + 鼠标滚轮水平也能翻搜索页）。键盘 ←/→/↑/↓ 已委托 moveSearchSelection 内部跨页。编译通过(BUILD SUCCEEDED)。注意：搜索翻页不跟手（松手后整页滑入，复用 pageTransitionOffset，未做相邻页跟手渲染）；gridGeometry 在搜索态由 searchResultsView 写入（origin 短时偏移无影响，仅取 width）。

- **提交+推送+打 tag 打包（次日 00:27 用户指令"提交代码 并push 打包"）**：`git commit` 搜索分页 5 个 Swift 文件 + 日志 → `295f968`（6 files changed, 247 insertions, 43 deletions）；`git push origin main`（1262c12..295f968）；`git tag v0.1.0 && git push origin v0.1.0` 触发工作流**完整运行**（含 DMG 打包 + 自动建 GitHub Release，CI 用自带 github.token）。新运行 **#30165686266**（run_number 3，事件 push，head 295f968c）排队中，后台监控任务 `9gbah4` 进行中。旧测试标签/Release `v0.0.0-ci-test` 仍未清理（含未签名 DMG 2.61MB），可后续 `gh release delete` + 删 tag 清理。

- **v0.1.0 打包验证通过（00:30）**：run #30165686266 全 12 步 success。API 二次确认：①artifact `AppLaunchpad-unsigned-dmg` 2.48 MB 已上传（未过期）；②GitHub Release `v0.1.0` 已自动创建并附 `AppLaunchpad.dmg` 2.50 MB，下载地址 https://github.com/YunyuVv/AppLaunchpad/releases/download/v0.1.0/AppLaunchpad.dmg。含搜索分页新功能的未签名 DMG 正式出包完成。仓库现共存 `v0.0.0-ci-test`(测试) 与 `v0.1.0`(正式) 两个 Release，旧测试标签可清理。
